name: Unit Tests
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: macos-12
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v2

    - run: swift test

